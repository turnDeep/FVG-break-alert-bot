# Discord Bot Token
# -----------------
# Discord Developer Portal (https://discord.com/developers/applications) から取得したトークンを設定してください。
DISCORD_BOT_TOKEN="YOUR_DISCORD_BOT_TOKEN_HERE"

# Discord Channel Name
# --------------------
# ボットがアラートを投稿するチャンネル名を設定します。
# このチャンネルが存在しない場合、ボットは起動時に自動で作成を試みます。
BOT_CHANNEL_NAME="fvg-break-alerts"

# Technical Analysis Parameters
# -----------------------------
# これらのパラメータは、`!optimize` コマンドによるML最適化で最適な値を見つけることができます。
# 最適化が完了すると、コンソールに推奨される設定値が出力されます。
# 初回実行時は、以下のデフォルト値が使用されます。

# 週足・日足の長期移動平均線の期間
MA_PERIOD=200

# FVG (Fair Value Gap) として検出する最小のギャップサイズ（パーセント）
FVG_MIN_GAP_PERCENT=0.5

# レジスタンスレベルを検出するために遡る期間（日数）
RESISTANCE_LOOKBACK=20

# レジスタンスを「突破」したと判断する価格の上昇率（例: 1.005 = 0.5%の上昇）
BREAKOUT_THRESHOLD=1.005

# ストップロスの割合（FVGの下限からの割合）
STOP_LOSS_RATE=0.02

# 目標利益の割合（エントリー価格からの割合）
TARGET_PROFIT_RATE=0.05

# 日足MA200に「付近」と判断する価格の乖離率
MA_PROXIMITY_PERCENT=0.05

# Real-time Scan Settings (for `realtime` mode)
# -----------------------------------------------
# リアルタイムモードで市場をスキャンする間隔（分）
SCAN_INTERVAL=15
